import React from "react";
// import NativeSelect from "@material-ui/core/NativeSelect";


const RegularSelectField = React.forwardRef((props, ref) => {   
    const {...rest} = props;
    return (
        <div></div>
            // <NativeSelect fullWidth {...rest} ref={ref}  defaultValue={''}>
            // </NativeSelect>
    );
});

export default RegularSelectField;